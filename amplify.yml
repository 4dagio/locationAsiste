version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm build --prod
  artifacts:
    baseDirectory: dist/asisteMapp
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
