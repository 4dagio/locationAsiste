version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run-script build
  artifacts:
    baseDirectory: platforms/www
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
